$(document).ready(function(){
       /*
        $('.datepicker').datepicker({
        format: 'yyyy-mm-dd'
        }).on('changeDate',function(ev){
           $('.datepicker').datepicker('hide');
        });
 */
 $("#new_insurence").on('submit',function(){
	alert("hello u clikced");
});
 
     });


